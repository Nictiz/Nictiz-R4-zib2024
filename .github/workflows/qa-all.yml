name: Profile QA - full repository
on: pull_request

jobs:
  nictiz-r4-qa-full:
    name: Loose QA on the entire repo
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: spy
        shell: bash
        run: echo ${{ github.event.pull_request.action }}
